# Fluorosis DL Project — Python Dependencies
# PyTorch (CUDA 13.0 nightly) must be installed separately via setup.sh
# because it requires the pytorch-nightly index.
# === pip install --pre torch torchvision --index-url https://download.pytorch.org/whl/nightly/cu130 ===

# Core ML
torch>=2.0
torchvision>=0.15
transformers>=4.30
scikit-learn>=1.3
scipy>=1.10

# Data
numpy>=1.24
pandas>=2.0
Pillow>=9.5
openpyxl>=3.1

# Training utilities (optional, for Kaggle notebooks)
matplotlib>=3.7
seaborn>=0.12
tqdm>=4.65
