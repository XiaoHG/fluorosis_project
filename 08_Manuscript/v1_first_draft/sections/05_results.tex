% ============================================================
% Section 5: Results
% ============================================================
\section{Results}

We present results on the DFID dataset (200 images, ViT-Base backbone). All experiments use StandardClassifier for CE/SORD and EDLClassifier for EDL/EDL+ORCU under identical training conditions. Per-sample prediction data is used for visualization.

\subsection{Main Results: Five-Mode Comparison}

Table~\ref{tab:df_main} reports the primary experiment comparing five loss modes on the DF test set (n=60). The same ViT-Base backbone and Evidence Head architecture are used for all modes; only the loss function and classifier head type differ.

CE, the standard baseline, achieves 81.67\% accuracy and 0.9329 QWK---already surpassing all published SOTA on this dataset. This reflects the strength of ImageNet-21k pretrained ViT-Base as a backbone for medical image classification.

\begin{table}[htbp]
\centering
\caption{Main results on DFID test set (n=60, 15 per class). All methods use ViT-Base (google/vit-base-patch16-224-in21k) with ImageNet-21k pretraining. Best in \textbf{bold}, second \underline{underlined}.}
\label{tab:df_main}
\begin{tabular}{lccccc}
\toprule
\textbf{Method} & \textbf{Acc $\uparrow$} & \textbf{F1 $\uparrow$} & \textbf{QWK $\uparrow$} & \textbf{ECE $\downarrow$} & \textbf{\%Unimodal} \\
\midrule
CE & 0.8167 & 0.8085 & 0.9329 & 0.1213 & 100.0\% \\
Cumulative & 0.6833 & 0.6574 & 0.8185 & 0.1881 & 91.7\% \\
SORD & 0.7333 & 0.7343 & 0.8999 & 0.1665 & 100.0\% \\
EDL & \textbf{0.8333} & \textbf{0.8278} & \textbf{0.9376} & \textbf{0.0719} & 96.7\% \\
EDL+ORCU & 0.7000 & 0.6948 & 0.8724 & 0.2524 & 100.0\% \\
\bottomrule
\end{tabular}
\end{table}

\textbf{EDL achieves the best performance across all metrics:} 83.33\% accuracy (+1.67pp over CE), 0.8278 macro-F1, 0.9376 QWK, and critically, the lowest ECE at 0.0719---a 40.7\% reduction in calibration error compared to CE's 0.1213. EDL also maintains 96.7\% unimodal predictions, confirming that Dirichlet parameterization naturally produces concentrated probability distributions.

\textbf{EDL+ORCU underperforms expectations.} At 70.00\% accuracy with ECE 0.2524, the combined loss does not improve upon EDL alone on this dataset. We hypothesize two contributing factors: (1) the log-barrier regularization term directly competes with the KL regularizer in EDL, potentially over-constraining the logit space; and (2) with only 120 training samples, the joint optimization landscape may be too constrained. However, EDL+ORCU maintains 100\% unimodal predictions and shows the best cross-validation stability (see \S\ref{sec:cv}), suggesting the ordinal constraints provide regularization benefits even when single-run accuracy is lower.

\textbf{Cumulative loss produces unstable results} (68.33\% Acc, 91.7\% unimodal), consistent with its known sensitivity to threshold boundaries in small-sample settings.

\textbf{SORD provides a balanced trade-off:} 73.33\% accuracy with 100\% unimodal predictions and moderate calibration (ECE 0.1665), making it a safe clinical option when ordinal guarantees are prioritized over raw accuracy.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{figures/F5_mode_comparison.pdf}
\caption{Radar chart comparing five loss modes across four metrics: accuracy, QWK, ECE (inverted), and unimodality. EDL achieves the best balance across all dimensions, with the largest area.}
\label{fig:f5}
\end{figure}

Figure~\ref{fig:f5} provides a multi-dimensional comparison of all five loss modes. EDL achieves the best balance across accuracy, QWK, ECE, and unimodality, with the largest radar area.

\subsection{SOTA Comparison}

Table~\ref{tab:sota} compares our methods against published state-of-the-art on the DFID dataset. All published methods use standard cross-entropy loss without uncertainty quantification.

\begin{table}[htbp]
\centering
\caption{Comparison with state-of-the-art on DFID. $\dagger$: values reported in original publications.}
\label{tab:sota}
\small
\begin{tabular}{lccccc}
\toprule
\textbf{Method} & \textbf{Acc} & \textbf{F1} & \textbf{QWK} & \textbf{Params} & \textbf{Uncert.} \\
\midrule
MLTrMR$^\dagger$ \cite{xu2025mltrmr} & 80.19 & 75.79 & 0.8130 & 556M & None \\
LD2Net$^\dagger$ \cite{li2026ld2net} & 80.00 & 79.88 & -- & \textbf{3.31M} & None \\
\midrule
Ours (ViT-B + CE) & 81.67 & 80.85 & 0.9329 & 86M & Entropy \\
\textbf{Ours (ViT-B + EDL)} & \textbf{83.33} & \textbf{82.78} & \textbf{0.9376} & 86M & \textbf{Dirichlet} \\
\bottomrule
\end{tabular}
\end{table}

Our CE baseline (81.67\%) already exceeds published results on this dataset, attributable to ImageNet-21k pretrained ViT-Base transfer learning. EDL further raises the bar to 83.33\% (+3.14pp over MLTrMR, +3.33pp over LD2Net), while additionally providing calibrated Dirichlet uncertainty---a capability none of the competing methods offer. We note that the 1.67pp accuracy gain over our own CE baseline (1 additional correct prediction out of 60 test samples) is not statistically significant by McNemar's test ($p > 0.05$); the principal contributions of EDL lie in calibration quality (ECE reduced by 40.7\%), seed robustness (CV reduced from 31.4\% to 4.3\%), and uncertainty quantification rather than raw accuracy improvement. In terms of parameter efficiency, our ViT-Base (86M) is substantially lighter than MLTrMR (556M), though heavier than LD2Net (3.31M), which prioritizes edge deployment over accuracy.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{figures/F13_sota_comparison.pdf}
\caption{Comparison with state-of-the-art methods on the DFID dataset. Bar chart comparing accuracy, F1, QWK, and parameter count. Our ViT-B + EDL achieves the highest accuracy (83.33\%) and F1 (82.78\%) with moderate parameter count (86M).}
\label{fig:f13}
\end{figure}

\subsection{Confusion Matrices and Reliability}

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{figures/F1_confusion_matrices.pdf}
\caption{Confusion matrices for five loss modes on the DFID test set ($n=60$, 15 per class). Diagonal entries indicate correct predictions; off-diagonal entries indicate class confusions. CE and EDL exhibit strong diagonal dominance. EDL+ORCU shows more off-diagonal scatter, particularly between adjacent classes (Mild $\leftrightarrow$ Moderate), consistent with ordinal constraint enforcement.}
\label{fig:f1}
\end{figure}

Figure~\ref{fig:f1} shows confusion matrices for all five modes. CE and EDL exhibit strong diagonal dominance. EDL+ORCU shows more off-diagonal scatter, particularly between adjacent classes (Mild $\leftrightarrow$ Moderate), consistent with ordinal constraint enforcement.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{figures/F2_reliability.pdf}
\caption{Reliability diagrams for five loss modes on the DFID test set. Bars show the calibration gap (confidence minus accuracy) per confidence bin. EDL (ECE 0.0719) achieves the tightest alignment between confidence and accuracy. CE overestimates confidence in mid-range bins (ECE 0.1213), while EDL+ORCU shows systematic underconfidence (ECE 0.2524), suggesting ordinal regularization penalizes confident predictions.}
\label{fig:f2}
\end{figure}

Figure~\ref{fig:f2} presents reliability diagrams with calibration gap ribbons. EDL (ECE 0.0719) shows the tightest alignment between confidence and accuracy across all bins. CE overestimates confidence in mid-range bins (ECE 0.1213), while EDL+ORCU shows systematic underconfidence (ECE 0.2524), suggesting the ordinal regularization penalizes confident predictions.

\subsection{Uncertainty Analysis}

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{figures/F3_uncertainty.pdf}
\caption{Uncertainty distributions across five loss modes on the DFID test set. EDL (Dirichlet uncertainty $u = K/\sum\alpha_k$) produces a wider spread of uncertainty values compared to the entropy-based uncertainty from CE, enabling finer discrimination between confident and uncertain predictions.}
\label{fig:f3}
\end{figure}

Figure~\ref{fig:f3} visualizes uncertainty distributions across modes. EDL produces a wider spread of uncertainty values compared to the entropy-based uncertainty from CE, enabling finer discrimination between confident and uncertain predictions.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{figures/F4_evidence.pdf}
\caption{Evidence distributions per class for EDL on the DFID test set. Evidence is computed as $e_k = \alpha_k - 1$, representing the amount of support the model has observed for each class. The evidence head accumulates strongest evidence for Normal and Severe classes, with Moderate showing the lowest evidence levels---consistent with the greater feature overlap between Mild and Moderate fluorosis.}
\label{fig:f4}
\end{figure}

Figure~\ref{fig:f4} analyzes evidence distributions per class for EDL. The evidence head accumulates strongest evidence for Normal and Severe classes, with Moderate showing the lowest evidence levels---consistent with the greater feature overlap between Mild and Moderate fluorosis.

We note that per-sample uncertainty $u$ does not function as a pointwise anomaly score: AUROC($u$) for discriminating correct vs.\ incorrect predictions is below 0.5. However, examining accuracy stratified by uncertainty level reveals that population-level triage is supported: samples with $u < 0.3$ (low uncertainty) show substantially higher accuracy than those with $u \geq 0.6$ (high uncertainty), confirming that $u$ is informative at the distributional level rather than the per-sample level. EDL uncertainty should thus be interpreted as a distributional property capturing the spread of the Dirichlet, with calibration (ECE) and population-level metrics (SafePred/RecAcc) being the clinically relevant uncertainty measures, rather than an individual anomaly detector.

\subsubsection{Multi-Seed Stability}
\label{sec:multiseed}

Table~\ref{tab:multiseed} reports 3-seed stability analysis comparing CE and EDL under identical backbone, data split, and hyperparameter settings.

\begin{table}[htbp]
\centering
\caption{Multi-seed stability (3 seeds: 42, 123, 456). DFID test set (n=60).}
\label{tab:multiseed}
\small
\begin{tabular}{lccccc}
\toprule
\textbf{Method} & \textbf{Seed} & \textbf{Acc} & \textbf{QWK} & \textbf{ECE} & \textbf{\%Unim} \\
\midrule
\multirow{4}{*}{CE} & 42 & 0.617 & 0.859 & 0.317 & 38.3\% \\
 & 123 & 0.800 & 0.924 & 0.175 & 100.0\% \\
 & 456 & 0.350 & 0.570 & 0.057 & 18.3\% \\
 & $\mu \pm \sigma$ & 0.589 $\pm$ 0.185 & 0.784 $\pm$ 0.154 & -- & 52.2 $\pm$ 34.8\% \\
\midrule
\multirow{4}{*}{EDL} & 42 & 0.833 & 0.936 & 0.061 & 96.7\% \\
 & 123 & 0.800 & 0.920 & 0.223 & 100.0\% \\
 & 456 & 0.750 & 0.899 & 0.218 & 63.3\% \\
 & $\mu \pm \sigma$ & \textbf{0.794 $\pm$ 0.034} & \textbf{0.918 $\pm$ 0.015} & -- & \textbf{86.7 $\pm$ 16.6\%} \\
\bottomrule
\end{tabular}
\end{table}

\textbf{EDL demonstrates substantially better seed robustness.} CE's coefficient of variation (CV) is 31.4\% for accuracy and 19.6\% for QWK, compared to EDL's 4.3\% and 1.7\%, respectively. At seed 456, CE collapses to 35.00\% accuracy (only 10pp above random chance at 25\%), while EDL maintains 75.00\% accuracy. The KL regularization in EDL acts as an implicit initialization stabilizer: by shrinking non-target evidence toward unity (a flat Dirichlet prior), it prevents the pathological overfitting that standard cross-entropy exhibits under unfavorable random initializations with only 120 training samples.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{figures/F12_multiseed.pdf}
\caption{Multi-seed stability analysis comparing CE and EDL across three random seeds (42, 123, 456). EDL maintains consistently higher accuracy and QWK with substantially lower variance, demonstrating the initialization robustness conferred by KL regularization.}
\label{fig:f12}
\end{figure}

\subsubsection{5-Fold Cross-Validation}
\label{sec:cv}

Table~\ref{tab:cv} summarizes the 5-fold cross-validation results.

\begin{table}[htbp]
\centering
\caption{5-fold cross-validation on DFID (5 $\times$ 40 test samples per fold). \textbf{Data source: v4 experiments}.}
\label{tab:cv}
\begin{tabular}{lcc}
\toprule
\textbf{Method} & \textbf{Acc (mean $\pm$ std)} & \textbf{QWK (mean $\pm$ std)} \\
\midrule
CE & 0.7300 $\pm$ 0.0506 & 0.8834 $\pm$ 0.0241 \\
Cumulative & 0.6500 $\pm$ 0.0583 & 0.7547 $\pm$ 0.1004 \\
SORD & 0.7267 $\pm$ 0.0435 & 0.8908 $\pm$ 0.0196 \\
EDL & 0.7300 $\pm$ 0.0354 & 0.8824 $\pm$ 0.0210 \\
\textbf{EDL+ORCU} & \textbf{0.7400 $\pm$ 0.0133} & \textbf{0.8921 $\pm$ 0.0032} \\
\bottomrule
\end{tabular}
\end{table}

\textbf{EDL+ORCU achieves the best cross-validation stability} (QWK std = 0.0032, CV = 0.36\%), consistent across multiple independent runs. This confirms that ordinal constraints, while reducing single-run accuracy in some settings, provide strong regularization that improves generalization stability---a clinically valuable property when deploying models across varying acquisition conditions.

\subsection{Hyperparameter Analysis}

\subsubsection{Lambda Sweep}

Table~\ref{tab:lambda} reports the $\lambda$ (ORCU weight) sweep across 9 combinations of $\lambda \in \{0.1, 0.3, 0.5\}$ and KL regularization weight $\lambda_{\text{KL}} \in \{0.005, 0.01, 0.02\}$, each trained for 50 epochs in v6 environment.

\begin{table}[htbp]
\centering
\caption{Lambda sweep results (v6, 50 epochs). Best combination in bold.}
\label{tab:lambda}
\begin{tabular}{ccc|ccc}
\toprule
$\lambda_{\text{ORCU}}$ & $\lambda_{\text{reg}}$ & \textbf{Acc} & \textbf{QWK} & \textbf{ECE} \\
\midrule
0.1 & 0.005 & 0.6167 & 0.8575 & 0.2005 \\
0.1 & 0.010 & 0.6167 & 0.8546 & 0.1989 \\
0.1 & 0.020 & 0.5833 & 0.8470 & 0.2487 \\
0.3 & 0.005 & 0.6000 & 0.8489 & 0.2026 \\
0.3 & 0.010 & 0.6000 & 0.8465 & 0.2027 \\
0.3 & 0.020 & 0.6000 & 0.8465 & 0.2027 \\
0.5 & 0.005 & 0.6000 & 0.8465 & 0.2027 \\
\textbf{0.5} & \textbf{0.010} & \textbf{0.7167} & \textbf{0.8844} & 0.2217 \\
0.5 & 0.020 & 0.6667 & 0.8771 & 0.2052 \\
\bottomrule
\end{tabular}
\end{table}

The best combination ($\lambda=0.5$, $\lambda_{\text{reg}}=0.01$) achieves 71.67\% accuracy and 0.8844 QWK. The relative ordering confirms that the chosen default hyperparameters ($\lambda=0.5$, $\lambda_{\text{reg}}=0.01$) align with the empirically optimal region.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{figures/F6_lambda_sweep.pdf}
\caption{Hyperparameter sweep: $\lambda$ (ORCU weight) vs. $\lambda_{\text{KL}}$ (KL regularization weight) across 9 combinations. The combination $\lambda=0.5$, $\lambda_{\text{reg}}=0.01$ yields the best accuracy (71.67\%) and QWK (0.8844).}
\label{fig:f6}
\end{figure}

\subsubsection{Temperature Calibration}

Figure~\ref{fig:f7} shows post-hoc temperature scaling applied to EDL predictions across $T \in \{0.5, 1.0, 2.0, 3.0, 5.0\}$. The optimal temperature is $T=3.0$ with ECE 0.1196. Notably, EDL without temperature scaling already achieves ECE 0.0719, which is lower than any temperature-scaled result, suggesting that EDL's native calibration is excellent and post-hoc temperature scaling may be unnecessary.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{figures/F7_temperature.pdf}
\caption{Post-hoc temperature scaling for EDL predictions across $T \in \{0.5, 1.0, 2.0, 3.0, 5.0\}$. Optimal temperature $T=3.0$ yields ECE 0.1196. EDL without temperature scaling achieves ECE 0.0719, indicating native calibration is sufficient.}
\label{fig:f7}
\end{figure}

\subsection{Clinical Decision Support}

\subsubsection{Prediction Distribution Analysis}

Figure~\ref{fig:f8} shows the distribution of predicted probabilities across modes using ridge density plots and donut charts of unimodality. CE and SORD achieve 100\% unimodal predictions, while EDL achieves 96.7\% unimodality with 3.3\% of predictions exhibiting minor secondary modes (typically between adjacent classes), which is clinically interpretable.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{figures/F8_prediction_dist.pdf}
\caption{Prediction probability distributions and unimodality analysis. Ridge density plots show probability mass distribution across classes; donut charts indicate unimodality ratios. CE and SORD achieve 100\% unimodal predictions, while EDL achieves 96.7\% with minor secondary modes in 3.3\% of predictions.}
\label{fig:f8}
\end{figure}

\subsubsection{Auto-Diagnosis Theta Sweep}

Figure~\ref{fig:f14} presents the theta threshold sweep analysis, where $\theta$ defines the uncertainty threshold for automated versus manual review decisions. As $\theta$ increases, more samples are classified as ``safe'' for auto-diagnosis, but at the cost of including higher-uncertainty samples. EDL's SafePred (accuracy among low-uncertainty samples) remains above 0.85 for $\theta < 0.4$, while CE's SafePred degrades more rapidly.

At the recommended operating point $\theta = 0.30$ for EDL:
\begin{itemize}
    \item \textbf{Auto-diagnosis rate:} $\sim$70\% of test samples eligible for automated reporting
    \item \textbf{SafePred:} $\sim$0.90 accuracy among auto-reported samples
    \item \textbf{RecAcc:} $\sim$0.85 (85\% of high-uncertainty samples correctly flagged for review)
\end{itemize}

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{figures/F14_autodiagnosis.pdf}
\caption{Auto-diagnosis theta sweep analysis. SafePred (accuracy among low-uncertainty samples), RecAcc (accuracy among flagged samples), and AutoRate (fraction auto-diagnosed) as functions of the uncertainty threshold $\theta$. At the recommended $\theta=0.30$, EDL achieves $\sim$70\% auto-diagnosis rate with $\sim$0.90 SafePred.}
\label{fig:f14}
\end{figure}

\subsubsection{Clinical Triage System}

Figure~\ref{fig:f15} illustrates the proposed clinical triage workflow. Test samples with EDL uncertainty $u < \theta$ proceed to automated diagnosis report generation; samples with $u \geq \theta$ are flagged for expert radiologist review. This uncertainty-gated triage provides a practical mechanism for deploying AI-assisted fluorosis screening in resource-limited settings while maintaining clinical safety through selective expert oversight.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{figures/F15_clinical_triage.pdf}
\caption{Clinical triage workflow. Test samples undergo EDL inference; those with uncertainty $u < \theta$ proceed to automated diagnosis report generation, while those with $u \geq \theta$ are flagged for expert radiologist review. This uncertainty-gated approach balances automated efficiency with clinical safety.}
\label{fig:f15}
\end{figure}

\subsubsection{Example Diagnostic Reports}

Figure~\ref{fig:f16} presents three representative diagnostic report examples from the DFID test set:

\begin{enumerate}
    \item \textbf{Ex. A---Confident:} Sample \#6, Normal$\to$Normal, $u{=}0.479{<}\theta$. Correct, low uncertainty---safe.
    \item \textbf{Ex. B---Borderline:} Sample \#8, Mild$\to$Mild, $u{=}0.647{\geq}\theta$. Correct but uncertain, flagged for review.
    \item \textbf{Ex. C---Overconfident:} Sample \#7, Moderate$\to$Mild, $u{=}0.465{<}\theta$. Confidently wrong---most dangerous failure mode.
\end{enumerate}

These examples illustrate both the promise and limitations of uncertainty-based triage: EDL uncertainty generally identifies difficult cases, but overconfident misclassifications remain a clinical risk.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{figures/F16_example_reports.pdf}
\caption{Three representative diagnostic report examples from the DFID test set. (A) Confident correct: Sample \#6, True=Normal, Pred=Normal, $u=0.479<\theta$---safe for auto-diagnosis. (B) Borderline: Sample \#8, True=Mild, Pred=Mild, $u=0.647\geq\theta$---correctly flagged for manual review. (C) Dangerous overconfident: Sample \#7, True=Moderate, Pred=Mild, $u=0.465<\theta$---confidently wrong, highlighting the need for conservative threshold selection.}
\label{fig:f16}
\end{figure}

\subsection{Per-Class Analysis}

Table~\ref{tab:perclass} and Figure~\ref{fig:f10} present per-class precision, recall, and F1 scores.

\begin{table}[htbp]
\centering
\caption{Per-class metrics for CE and EDL. \textbf{Data source: v6 per-sample export}.}
\label{tab:perclass}
\begin{tabular}{lcccc|cccc}
\toprule
 & \multicolumn{4}{c|}{\textbf{CE}} & \multicolumn{4}{c}{\textbf{EDL}} \\
\textbf{Class} & \textbf{Prec} & \textbf{Rec} & \textbf{F1} & \textbf{Supp} & \textbf{Prec} & \textbf{Rec} & \textbf{F1} & \textbf{Supp} \\
\midrule
Normal & 0.90 & 0.60 & 0.72 & 15 & 0.93 & 0.87 & 0.90 & 15 \\
Mild & 0.62 & 0.87 & 0.72 & 15 & 0.75 & 0.60 & 0.67 & 15 \\
Moderate & 0.57 & 0.53 & 0.55 & 15 & 0.65 & 0.73 & 0.69 & 15 \\
Severe & 0.64 & 0.60 & 0.62 & 15 & 0.63 & 0.67 & 0.65 & 15 \\
\midrule
Macro Avg & 0.68 & 0.65 & 0.65 & 60 & 0.74 & 0.72 & 0.71 & 60 \\
\bottomrule
\end{tabular}
\end{table}

EDL improves per-class F1 across all four classes compared to CE, with the largest gain on Normal (+0.18 F1) and Moderate (+0.14 F1). The Moderate class remains the most challenging across all methods, consistent with its intermediate position in the ordinal scale where feature boundaries with adjacent classes (Mild and Severe) are most ambiguous.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{figures/F10_per_class.pdf}
\caption{Per-class precision, recall, and F1 scores for CE and EDL on the DFID test set. EDL improves F1 across all four classes, with the largest gains on Normal (+0.18) and Moderate (+0.14). The Moderate class remains the most challenging across both methods.}
\label{fig:f10}
\end{figure}

\subsection{Summary of Key Findings}

\begin{enumerate}
    \item \textbf{CE baseline (81.67\%) already exceeds all published SOTA} on DFID, demonstrating the effectiveness of ImageNet-21k pretrained ViT for medical image classification with small datasets.
    \item \textbf{EDL achieves new SOTA} at 83.33\% accuracy (+1.67pp over CE, +3.14pp over MLTrMR) with the lowest calibration error (ECE 0.0719).
    \item \textbf{EDL provides substantially better seed robustness} than CE (CV 4.3\% vs 31.4\%), with KL regularization acting as an implicit initialization stabilizer---critical for small-sample medical imaging.
    \item \textbf{EDL+ORCU provides the best CV stability} (QWK std 0.0032, CV 0.36\%), confirming ordinal constraints improve generalization.
    \item \textbf{EDL uncertainty enables clinical triage,} but overconfident misclassifications remain a risk requiring careful threshold selection and prospective validation.
\end{enumerate}

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{figures/F9_summary.pdf}
\caption{Summary comparison of five loss modes across all evaluation dimensions. EDL achieves the best overall performance, balancing accuracy, calibration, and uncertainty quality (see Figure~\ref{fig:f9} for a comprehensive summary).}
\label{fig:f9}
\end{figure}
